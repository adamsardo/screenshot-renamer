# Project instructions

Build with Xcode 27 or newer. Use per-command DEVELOPER_DIR when needed; do not change the global selection. Keep all image processing local. Never test renames on original personal files. Use disposable fixtures. Review file-operation changes for overwrites, identity checks and durable recovery. No secrets, personal screenshots or local evaluation manifests in Git. Run Swift tests and build the packaged app before shipping. Clearly distinguish ad-hoc previews from notarised distribution.
